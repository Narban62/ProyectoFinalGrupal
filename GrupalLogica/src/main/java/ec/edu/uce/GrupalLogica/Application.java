package ec.edu.uce.GrupalLogica;

//import ec.edu.uce.GrupalLogica.view.AdminUI;
//import ec.edu.uce.GrupalLogica.view.ClienteUI;



import javax.swing.*;
import java.awt.*;


public class Application  {

   /* public static void main(String[] args) {

        if (!GraphicsEnvironment.isHeadless()) {
            ProductService productService = context.getBean(ProductService.class);
            ProductFinalService productFinalService = context.getBean(ProductFinalService.class);

            SwingUtilities.invokeLater(() -> {
                AdminUI adminUI = new AdminUI(productService, productFinalService);
                adminUI.mostrar();

                ClienteUI clienteUI = new ClienteUI(productService);
                clienteUI.mostrar();
            });
        } else {
            System.out.println("Running in headless mode. UI components will not be initialized.");
        }
    }*/
}
